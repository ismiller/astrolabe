using Astrolabe.AppSDK.Controls;

namespace Test;

/// <summary>
/// An empty page that can be used on its own or navigated to within a Frame.
/// </summary>
public sealed partial class TestPage : AstrolabePage
{
    public TestPage()
    {
        this.InitializeComponent();
    }
}